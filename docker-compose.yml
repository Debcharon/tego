services:
  app:
    build: .
    image: tego:local
    environment:
      BOT_TOKEN: ${BOT_TOKEN:-}
    volumes:
      - ./data:/app/data  # Modify the path to the local data directory
    restart: unless-stopped
